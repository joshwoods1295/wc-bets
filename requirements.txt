soccerdata>=1.8,<1.9  # 1.9 dropped defense/passing_types from the player-season API (need tackles + corners)
pandas>=2.0,<3        # soccerdata 1.8 is incompatible with pandas 3.x MultiIndex handling
streamlit>=1.30
pytest>=8.0
